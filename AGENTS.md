# AGENTS.md

## Cursor Cloud specific instructions

This repository (`Ideasgohereduh`) is an early-stage brainstorming/idea repository for a platformer game. As of the latest setup (March 2026), it contains **no application code, no dependencies, no build system, and no services**.

- **No languages/frameworks** are used yet — there are no `package.json`, `requirements.txt`, `Makefile`, `Dockerfile`, or any other dependency/build configuration files.
- **No lint, test, or build commands** exist.
- **No services** need to be started.
- The repository consists of `README.md` and `Ideasforplatformlevels` (a brainstorming text file).

When code is eventually added, this section should be updated with setup, build, test, and run instructions.
